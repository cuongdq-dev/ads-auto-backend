export enum ServiceStatusEnum {
  INSTALLED = 'installed',
  INSTALLING = 'installing',
  UN_INSTALLED = 'uninstalled',
  FALED = 'failed',
}

export enum workspaceEnum {
  wp_system = 'SYSTEM',
  wp_news = 'POST',
  wp_books = 'BOOK',
}
